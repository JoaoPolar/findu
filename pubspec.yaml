name: findu_admin
description: "Sistema administrativo FindU - Gestão de ensalamento"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.8.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  http: ^1.1.0
  uuid: ^4.5.1
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
